package ar.edu.unq.inscripcionunq.spring.controller;

import org.springframework.web.bind.annotation.RestController;

@RestController
public class CareerController {
	/*
	 * @Autowired private GenericService<Career> careerService;
	 * 
	 * @PostMapping("/career") public ResponseEntity<?> save(@RequestBody Career
	 * career) { long id = careerService.save(career); return
	 * ResponseEntity.ok().body("Nueva Carrera creada ID:" + id); }
	 * 
	 * @GetMapping("/career/{id}") public ResponseEntity<Career>
	 * get(@PathVariable("id") long id) { Career career = careerService.get(id);
	 * return ResponseEntity.ok().body(career); }
	 * 
	 * @GetMapping("/career") public ResponseEntity<List<Career>> list() {
	 * List<Career> careers = careerService.list(); return
	 * ResponseEntity.ok().body(careers); }
	 * 
	 * @PutMapping("/career/{id}") public ResponseEntity<?>
	 * update(@PathVariable("id") long id, @RequestBody Career career) {
	 * careerService.update(id, career); return
	 * ResponseEntity.ok().body("Carrera actualizada."); }
	 * 
	 * @DeleteMapping("/career/{id}") public ResponseEntity<?>
	 * delete(@PathVariable("id") long id) { careerService.delete(id); return
	 * ResponseEntity.ok().body("Carrera borrada"); }
	 */
}